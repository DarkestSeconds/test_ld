import cacache from 'cacache';

// Paths for Cache
const projectPath = './Cache/Project';
const userPath = './Cache/User/';

class CachingController {

    // vou deixar para criar depois...

    // async numberOfProjectsPerType() {

    //     cacache.put(projectPath, 'projectsNumbersPerType', );

    // }

}